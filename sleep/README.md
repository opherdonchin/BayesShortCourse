# Sleep deprivation notebooks

These are the self-work versions of the Sleep Deprivation sequence: the same setup, data loading, plotting helpers, and numbered scientific questions as `solved/`, with the model-building and analysis code left for students to fill in. Outputs are intentionally cleared, consistent with `AGENTS.md`.

See `solved/README.md` for the full notebook sequence, the plotting grammar shared with `golf/`, and modeling notes.
